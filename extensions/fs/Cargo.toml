[package]
name = "lenz_extension_fs"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib", "cdylib"]
name = "lenz_extension_fs"

[dependencies]
lenz_core={path = "../../agent/core"}
bytes = {workspace = true}
tokio = {workspace = true}
