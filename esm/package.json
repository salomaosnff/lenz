{
  "name": "esm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:js": "rm -rf dist && tsx build.ts",
    "build:types": "rm -rf types && tsc -p tsconfig.types.json",
    "build:copy-dts": "for file in src/*.d.ts src/**/*.d.ts; do cp $file types/$(basename $file); done",
    "build": "pnpm build:js && pnpm build:types && pnpm build:docs",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:serve": "vitepress serve docs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "fast-glob": "^3.3.2",
    "tsx": "^4.19.0",
    "typedoc": "^0.26.7",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "esbuild": "^0.23.1",
    "vitepress": "1.3.4",
    "vue": "3.5.8"
  }
}
